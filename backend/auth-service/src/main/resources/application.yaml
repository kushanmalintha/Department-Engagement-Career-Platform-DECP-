server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/decp_auth
    username: postgres
    password: kushan

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
